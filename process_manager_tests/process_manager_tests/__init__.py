"""
process_manager_tests — test suite for the process_manager package.

Structure:
    active/    — tests that run
    obsolete/  — retired tests, excluded from discovery
    todo/      — empty stubs marking gaps in coverage

Usage:
    python -m unittest discover -s process_manager_tests/active -v
"""
